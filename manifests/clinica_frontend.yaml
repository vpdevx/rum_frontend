apiVersion: apps/v1
kind: Deployment
metadata:
  name: cl-frontend
  labels:
    app: cl-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cl-frontend
  template:
    metadata:
      labels:
        app: cl-frontend
    spec:
      containers:
      - name: cl-frontend
        image: vinicpires/clinica_frontend:SHORT_SHA
        ports:
        - containerPort: 80
--- 

apiVersion: v1
kind: Service
metadata:
  name: cl-frontend-service
  labels:
    app: cl-frontend
spec:
  selector:
    app: cl-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

--- 

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cl-frontend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: "cloudflare-cluster-issuer"
spec:
  rules:
    - host: cl-frontend.k8s.lab4ever.xyz  # Substitua pelo seu domínio
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cl-frontend-service
                port:
                  number: 80
  tls:
    - hosts:
        - cl-frontend.k8s.lab4ever.xyz
      secretName: cl-frontend-tls
